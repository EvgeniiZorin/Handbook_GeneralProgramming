# The simplest docker image imaginable!

Just a `main.py` file to run. That's it!

## Build docker image

`docker build -t 230602_example1 .`

## Run a container from image

`docker run 230602_example1`

